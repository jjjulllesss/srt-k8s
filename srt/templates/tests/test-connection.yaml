apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "srt.fullname" . }}-test-connection"
  labels:
    {{- include "srt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "srt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
